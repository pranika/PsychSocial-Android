<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="posts.facebook.pranika.facebookapi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".DaggerApp.DaggerApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FrontPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".MainActivity" />
        <activity android:name=".Feeds" />
        <activity android:name=".SingleFeed" />
        <activity android:name=".MonthFeed" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".TabbedActivity"
            android:label="@string/title_activity_tabbed"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisteredActivity" />
        <activity
            android:name=".BottomNavigation"
            android:launchMode="singleTop">

            <!-- Branch URI scheme -->
            <intent-filter>
                <data
                    android:host="open.my.app"
                    android:scheme="http" />

                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailActivity" />
        <activity android:name=".DetailWeekActivity" />
        <activity android:name=".DetailYearActivity" />
        <activity android:name=".AuthAffectedPatients" />
        />
        <activity android:name=".PatientStatus" />
        <activity android:name=".WeekGraph" />
        <activity android:name=".DoctorSignUp" />
        <activity android:name=".ShowDoctors" />
        <activity android:name=".OrganizationSignUp" />
        <activity android:name=".OrganizationLogin" />
        <activity android:name=".UpdateDoctor" />
        <activity android:name=".BottomNavigationOrganization" />

        <!-- Branch init -->
        <!-- <meta-data android:name="io.branch.sdk.BranchKey" android:value="key_live_aeCQFMC3MWke3ctAeTdX4lmbsEj1pwEt" /> -->
        <!-- <meta-data android:name="io.branch.sdk.BranchKey.test" android:value="key_test_bkCVuTqXL4od2bzupGnZWfpnstf6oFqG" /> -->


        <!-- &lt;!&ndash; Branch testing (TestMode "true" to simulate fresh installs on dev environment) &ndash;&gt; -->
        <!-- <meta-data android:name="io.branch.sdk.TestMode" android:value="true" /> -->


        <!-- Branch install referrer tracking -->
        <!-- <receiver android:name="io.branch.referral.InstallListener" android:exported="true"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.android.vending.INSTALL_REFERRER" /> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->
        <!-- <service android:name=".FcmInstanceIdService"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.google.firebase.INSTANCE_ID_EVENT" /> -->
        <!-- </intent-filter> -->
        <!-- </service> -->
        <!-- <service android:name=".FcmMessagingService"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.google.firebase.MESSAGING_EVENT" /> -->
        <!-- </intent-filter> -->
        <!-- </service> -->

        <activity android:name=".AffectedPatients" />
        <activity
            android:name=".PhoneVerification"
            android:label="@string/title_activity_phone_verification"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>